// import { InspirationImageI } from "../utils/interfaces/interfaces";

// export default function CarouselImage({ id, URL, label }: InspirationImageI) {
//   return (
    // <div key={id} className="_slot">
    //   <img src={`http://localhost:8000${URL}`} alt="image in the carousel" />
    //   <p className="legend">{label}</p>
    // </div>
//   )
// }